#include <bits/stdc++.h>
using namespace std;

int n;

int main(){
    cin >> n;

    for (int i = 0;i < n; i++){
        string s;
        cin >> s;
        stack<char> st;
        for(char c : s){
            if (st.empty())
                st.push(c);
            else{
                ABAB
            }
        }
    }

    return 0;
}